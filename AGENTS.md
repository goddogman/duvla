# DuVLA 协作与发布规范

正式展示名为 DuVLA；仓库、项目目录和 Python 包名统一为 `duvla`。
V3.31是唯一对外主模型，完整LIBERO 2000集为1906/2000（95.30%）；其余版本只作本地实验。
项目自有代码/策略采用MIT；作者暂为DuVLA contributors，GitHub账号确定后更新署名。

## 工作范围

- 修改前说明计划和验证方法。只要求分析时不启动训练、测评、下载或清理。
- 保留用户已有改动；不擅自提交、推送、创建远程仓库或改变许可证。
- 每次完成一个有边界的任务；测试必须针对实际改动。失败时先解释原因，不删除重装。
- 若本地存在 `AGENTS.local.md`，必须同时阅读并遵循其中研究、硬件和恢复规则；
  该文件只用于本机，不进入公开发布包。用户最新明确要求优先。
- 研究记录使用中文，追加到本地忽略的 `docs/learning_log.md`；历史证据保留。
  Git 提交不包含实验数据、逐集结果、模型卡草稿、研究日志或发布计划。
- DPV4/DS4VLA 是独立路线，不混用其模型、数据和结果；本地说明保留在 `docs/dpv4_vla.md`。

## 模型与数据契约

- 图像方向、相机顺序、状态定义、动作维度/符号、时间对齐和归一化不得静默修改。
- 结构、张量形状、梯度路径、保存加载和训练—推理一致性必须有对应测试。
- 参数量分别报告冻结 Qwen、全部 policy、激活路径与当前训练参数。
- V3.31 的主策略是 Flow Matching；不要把未启用的残差模块、Outcome Planner 或 Recovery
  称为当前发布模型的能力。不得将LoRA实验列为此次发布模型。
- 训练只用允许的训练示范。official eval 初始状态、成功信号、奖励、视频与轨迹不进入梯度训练。
  任务编号仅用于加载环境、审计和汇总，不能传给策略选专家。
- 全示范 refit 要明确声明 2000 个训练 episodes、无示范 validation；
  400/2000 闭环结果属于开发测评，不冒称盲测、未见任务或真实机器人泛化。
- 原始 Qwen、数据、权重、缓存和训练输出不因版本较旧自动删除；删除需用户确认精确清单。

## 复现与资源

- 优先兼容单张 8GiB 消费级 GPU；训练缓存与在线推理成本分开报告。
- 项目默认离线加载 Qwen；用 `DUVLA_QWEN_PATH` 指定本地路径，不自动下载大型资产。
- 公开入口不得依赖作者的绝对路径；使用命令行参数、环境变量或相对于源码的位置。
  本机只保留`projects/duvla`一个项目目录；历史权重、manifest和结果内的旧路径/hash是来源证据，
  不批量改写，也不能假定旧路径仍可访问。
- 测评依赖保持 `transformers==4.57.6`、`huggingface_hub==0.36.2`、
  `tokenizers==0.22.2`。不擅自升级现有环境、驱动或 CUDA。
- 同协议比较固定 20Hz、双相机128、K5、action2、seed23、自然语言；
  时限 LIBERO-10/Spatial/Object/Goal = 520/280/280/300。
- 完整结果须覆盖 40 tasks × 50 states；部分结果注明分母，不能外推为完整成功率。
- 目标训练默认每5000 optimizer updates检查一次，用户询问、异常和阶段切换除外。
  保存 loss 曲线、实际曝光与恢复状态。受限工具看不到GPU不等于后台任务停止。

## 开源准备

- GitHub 保存代码、测试、公开配置和使用文档；训练数据、缓存、checkpoint、
  逐集测评结果、结果JSON、发布草稿与研究日志均只在本机保留。
- 用户要求研究与公开内容保留在同一目录；不为精简仓库搬移测试、DPV4、日志或输出。
  用 `.gitignore` 区分公开文件和本地研究。新增公开脚本/测试须同步忽略例外及发布允许列表。
- 直接在当前仓库开发并提交；提交前检查暂存文件清单和差异，不强制加入被忽略的本地资料。
  `scripts/prepare_public_release.py` 是本地工具，不纳入 Git 提交。
- 保留训练来源/协议/哈希，公开副本可移除私人绝对路径；原始文件不改。
  重打包 SHA256 改变时必须记录原始与发布 hash、并核验全部权重张量相等。
- PyTorch checkpoint 只能来自可信来源并使用 `weights_only=True`；
  当前不是 Transformers 的 `AutoModel.from_pretrained` 模型。
- LICENSE、作者署名、代码来源审计、模拟器来源、干净机器复现通过后再公开。
  不把第三方代码许可自动等同于所有训练资产的再分发许可。
- README 只保留项目定位、当前架构、可核实的汇总指标、使用入口和限制；
  私人实验记录与详细发布计划留在本地，复现说明见 `docs/reproduction.md`。
